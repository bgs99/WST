plugins {
    id 'java'
    id 'jacoco'
    id 'war'
    id "io.freefair.lombok" version "6.4.3"
}

description = 'WST service'

dependencies {
    compileOnly group: 'jakarta.platform', name: 'jakarta.jakartaee-api', version: '9.1.0'
    implementation project(':shared')
}
